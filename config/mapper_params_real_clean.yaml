slam_toolbox:
  ros__parameters:

    # Solver
    solver_plugin: solver_plugins::CeresSolver
    ceres_linear_solver: SPARSE_NORMAL_CHOLESKY
    ceres_preconditioner: SCHUR_JACOBI
    ceres_trust_strategy: LEVENBERG_MARQUARDT
    ceres_dogleg_type: TRADITIONAL_DOGLEG
    ceres_loss_function: None

    # Frames and scan topic
    odom_frame: odom
    map_frame: map
    base_frame: base_link
    scan_topic: /scan
    mode: mapping
    use_map_saver: true

    # Stable real-time behavior
    debug_logging: false
    throttle_scans: 1
    transform_publish_period: 0.02
    transform_timeout: 0.30
    tf_buffer_duration: 30.0
    stack_size_to_use: 40000000
    enable_interactive_mode: true
    scan_queue_size: 1

    # Map update rate: responsive but tf_buffer_duration: 30.0
    stack_size_to_use: 40000000
    enable_interactive_mode: true
    scan_queue_size: 1

    # Map update rate: responsive but not noisy
    map_update_interval: 0.40
    minimum_time_interval: 0.15

    # Indoor LiDAR filtering
    resolution: 0.05
    min_laser_range: 0.15
    max_laser_range: 5.0

    # Scan insertion: enough movement for a trustworthy scan
    use_scan_matching: true
    use_scan_barycenter: true
    minimum_travel_distance: 0.08
    minimum_travel_heading: 0.08

    # Local scan matching
    scan_buffer_size: 10
    scan_buffer_maximum_scan_distance: 5.0
    link_match_minimum_response_fine: 0.15
    link_scan_maximum_distance: 1.5

    # Keep disabled while odometry is being calibrated.
    # This avoids large map-wide corrections and visible map shifting.
    do_loop_closing: false
    loop_search_maximum_distance: 3.0
    loop_match_minimum_chain_size: 10
    loop_match_maximum_variance_coarse: 3.0
    loop_match_minimum_response_coarse: 0.35
    loop_match_minimum_response_fine: 0.45

    # Correlative scan matcher
    correlation_search_space_dimension: 0.50
    correlation_search_space_resolution: 0.01
    correlation_search_space_smear_deviation: 0.10

    # Loop-closure matcher values, used later if enabled
    loop_search_space_dimension: 8.0
    loop_search_space_resolution: 0.05
    loop_search_space_smear_deviation: 0.03

    # Motion penalties
    distance_variance_penalty: 0.50
    angle_variance_penalty: 1.00
    fine_search_angle_offset: 0.00349
    coarse_search_angle_offset: 0.349
    coarse_angle_resolution: 0.0349
    minimum_angle_penalty: 0.90
    minimum_distance_penalty: 0.50
    use_response_expansion: true

    # Occupancy-grid filtering
    min_pass_through: 2
    occupancy_threshold: 0.10
